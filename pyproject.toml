[project]
name = "jarvis-v5"
version = "0.1.0"
description = "JARVIS V5 State Foundation — canonical state, atomic mutations, obligations, evidence"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]  # flat layout: the v5 package lives at repo root; pytest>=7 officially supports this
addopts = "-q"
